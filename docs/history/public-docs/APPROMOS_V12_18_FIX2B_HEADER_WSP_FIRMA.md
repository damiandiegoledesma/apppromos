# AppPromos V12.18-FIX2B — Header + WhatsApp + firma AppPromos

## Correcciones
1. Corrige bug bloqueante `customerNote is not defined` al enviar pedido.
2. Header mobile simplificado:
   - logo algo más grande;
   - nombre;
   - una sola línea `dirección · localidad · provincia`;
   - botón oficial de WhatsApp a la derecha;
   - sin repetir teléfono visible.
3. Mantiene limpia la foto del frente (sin identidad duplicada).
4. Reemplaza la antigua tarjeta mínima `Con AppPromos` por una firma pasiva:
   `Esta carnicería online funciona con AppPromos` + link a la landing.

## Preserva
- MOBILE-UX integral.
- Snapshot público autónomo.
- FIX2-A: rubros, ofertas compactas, contenido promo en carrito, sin aclaración opcional.
- Firebase/Auth/Storage/Firestore sin cambios.
