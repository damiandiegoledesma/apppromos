# V12.7.2-A.2 — Fix carga de app en demo

## Problema
La app quedaba frenada con:

`The requested module '../services/data-service.js' does not provide an export named 'updateProductPricesBatch'`

## Solución
Se agrega `updateProductPricesBatch` a `public/js/services/data-service.js`.

## Seguridad demo
En modo demo, los cambios de precios/productos quedan guardados en `localStorage` y no pisan Firebase ni la empresa demo.

## Alcance
No toca Auth, Firebase rules, BusinessStore profundo, web automática, tracking ni backend.
